{
  "$schema": "https://json.schemastore.org/package",
  "name": "uswift-monorepo",
  "private": true,
  "scripts": {
    "build": "cd dashboard && npm ci && npm run build",
    "start": "cd dashboard && npm run start"
  },
  "workspaces": [
    "dashboard",
    "extension"
  ]
}